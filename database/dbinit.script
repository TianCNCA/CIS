CREATE TABLE CLIENTS ( ID INTEGER NOT NULL PRIMARY KEY, NAME VARCHAR( 30 ) NOT NULL UNIQUE, DOB VARCHAR( 8 ), HOMEPHONE VARCHAR( 18 ), WORKPHONE VARCHAR( 21 ), ADDRESS VARCHAR( 30 ), CITY VARCHAR( 20 ), PROVINCE VARCHAR( 4 ), POSTALCODE VARCHAR( 7 ), PHYSICHIAN BOOLEAN, PHYSTHER BOOLEAN, CHIRO BOOLEAN, PREVEXP BOOLEAN, REASON VARCHAR( 100 ), DIET BOOLEAN, MED BOOLEAN, INSULIN BOOLEAN, UNCTRL BOOLEAN, OCCUPATION VARCHAR( 20 ), SPORTS VARCHAR( 30 ), SLEEP VARCHAR( 20 ), SMOKING INTEGER, ALCOHOL INTEGER, STRESS INTEGER, APPETITE INTEGER )

CREATE TABLE SOAPS ( ID INTEGER NOT NULL PRIMARY KEY, NAME VARCHAR( 30 ) NOT NULL, DATE VARCHAR( 30 ) NOT NULL, DISC VARCHAR( 1024 ) NOT NULL UNIQUE )

CREATE TABLE ID ( KEY INTEGER NOT NULL PRIMARY KEY, ID INTEGER NOT NULL )

/*CREATE TABLE SOAPS ( NAME VARCHAR(30) NOT NULL PRIMARY KEY, )*/

CREATE USER SA PASSWORD "" ADMIN